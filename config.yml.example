---
postgresql:
    username: bodgery
    passwd: 
    database: bodgery
    host: localhost
    port: 5432

memberpress:
    user: bodgery
    passwd: bodgery
    base_url: https://mms.thebodgery.org

password_storage:
    type: bcrypt
    bcrypt:
        difficulty: 10

# Create key with:
# python -c 'import secrets; print(secrets.token_hex())'
session:
  key:
  life_minutes: 60

build_id:
build_branch:
build_date:
